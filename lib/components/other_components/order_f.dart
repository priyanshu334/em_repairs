// lib/components/order_field.dart
import 'package:flutter/material.dart';

class OrderF extends StatelessWidget {
  const OrderF({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Order Field Component");
  }
}